# Codex 一次性开发任务

请在一个全新的专用仓库中实现 `TFT S18 仙灵快速检索器`。

## 最高优先级

1. 严格阅读并遵循 `SPEC.md`、`DATA_SCHEMA.md`、`RULES.md`、`SOURCE_POLICY.md`、`ACCEPTANCE_TESTS.md`。
2. 不自行扩大产品范围，不把它做成百科站。
3. UI 以 `ui/UI_REFERENCE.png` 为方向参考，但必须遵循 `SPEC.md` 中最新文字规则；图片中的分页、历史追踪等早期草案如果与文字规格冲突，以文字规格为准。
4. 所有仙灵内容都必须来自 JSON 数据层，不得写死在 UI 组件。
5. 数据、规则、搜索逻辑、概率逻辑、UI 必须解耦。

## 技术要求

- Vite
- Vanilla TypeScript
- CSS
- JSON 数据
- 静态站点，可直接部署 GitHub Pages
- 不需要后端
- 移动端与桌面端响应式

## V1 功能

- 大搜索框：搜索名称、普通效果、Blossom 升级、Prismatic 效果、条件、内部概念、同义词。
- 公共筛选：精确回合/阶段范围、当前金币、官方 7 类、仅棱彩、效果展示模式。
- 高级筛选：参考仙灵的阶段重叠窗口、售价范围、特殊条件筛选、排序。
- 连续卡片瀑布/网格，一页向下滚动，不分页。
- 卡片折叠态显示：名称、官方类型视觉、费用、阶段、普通/当前效果摘要、出现条件。
- 卡片展开态显示完整普通/升级/棱彩、所有出现条件、限制、完整阶段区间、来源元数据。
- 概率模式默认关闭；打开后实时计算理论等权概率。
- 每张卡提供“手动排除”控件，排除后候选池和概率立即重算。
- Stage 5+ 概率模式支持 `普通位 / 强制 Combat 位 / 不确定` 三种状态。
- 单独“刷新规律”页面，规则带可信度标签：官方确认 / 高置信观察 / 未确认。

## 数据工具

实现：

- `scripts/validate-data.ts`
- `scripts/diff-data.ts`

推荐预留但无需在第一提交完成自动网络抓取：

- `scripts/build-data.ts`

`validate-data` 必须校验 ID、类别、费用、阶段范围、条件结构、Prismatic nullable、来源元数据。

`diff-data` 必须能够比较两个版本并输出字段级变更。

## 完成标准

- `npm install`
- `npm run dev`
- `npm run build`
- `npm test`

全部通过。

完成后给出：

1. 文件树；
2. 本地启动步骤；
3. GitHub Pages 部署步骤；
4. 已实现功能；
5. 与规格仍有差异的地方（如有）；
6. 测试结果。
