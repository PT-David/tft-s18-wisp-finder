# 产品规格 SPEC — TFT S18 仙灵快速检索器

## 1. 核心目标

对局查询优先于资料浏览。用户打开页面后不应先面对几百条仙灵百科，而应先看到搜索和公共游戏条件。

## 2. 页面结构

V1 两个一级模块：

- `仙灵查询`
- `刷新规律`

不加入登录、收藏、社区、排行榜等功能。

## 3. 查询页顶部

### 3.1 主搜索框

视觉中心。占据明显宽度。

Placeholder：`搜索仙灵名称或效果……`

搜索范围：

- 中文名
- 英文名（第一版可隐藏，但参与索引）
- 普通效果
- Blossom 升级效果
- Prismatic Blossom 效果
- 出现条件文本
- 隐藏搜索概念
- 同义词

多个普通关键词默认 AND。

### 3.2 公共筛选

只放不需要“理解仙灵内容”即可定义的客观字段：

- 当前回合 / 精确回合
- 阶段范围
- 当前金币
- 官方类型（7 类）
- `仅棱彩`
- 重点效果模式：普通 / Blossom 升级 / Prismatic（只改变视觉强调；卡片常驻信息不因此被隐藏）
- 概率模式开关

不要把 `复制器 / 玩家生命 / 阵亡 / 击杀` 等内容词做成首页快捷筛选；它们属于搜索系统。

## 4. 阶段查询

### 4.1 精确回合

若仙灵存在任一 `stage_range` 包含当前回合，则通过阶段过滤。

### 4.2 范围查询

若仙灵任一出现区间与查询范围有非空交集，则显示。

### 4.3 参考仙灵重叠窗口

不要使用歧义按钮“查看同期”。

高级筛选可选择一个参考仙灵 A，并生成查询区间：

`A 的出现区间 ∩ [当前回合, +∞)`

候选仙灵 B 只要任一出现区间与上述区域有交集，即满足。

示例：

- A：3-5～4-7
- B：4-2～4-5
- 当前：4-3

B 应被显示，因为 4-3～4-5 存在交集。

## 5. 当前金币

`currentGold` 属于游戏状态，不等同于单纯的“费用筛选”。

V1 候选池可以根据已知的“可负担条件”排除明确买不起的仙灵；但字段应设计为可配置规则，不要把所有复杂条件写死为 `cost <= currentGold`。

另提供纯浏览性质的 `售价范围` 高级筛选。

## 6. 结果展示

### 6.1 不分页

所有搜索结果在同一结果页连续向下滚动。桌面 3～4 列；移动端 1～2 列。

导航栏可保持轻量 sticky；完整查询筛选区域必须随页面滚动，不得长期遮挡结果。

### 6.2 卡片常驻信息

- 中文名
- 官方类型图标/颜色
- 费用
- 出现阶段（支持多区间）
- 普通效果
- Blossom Upgrade（存在时）
- **出现条件：若有，折叠态必须可见**
- once-per-game / cooldown 等限制标签
- 概率（仅概率模式）
- 排除复选框（仅概率模式）

### 6.3 按需展开信息

Prismatic Blossom 存在时放在紧凑的独立折叠区，不存在时不显示空区块。来源、可信度和核验日期放在单独的“数据来源”折叠区。折叠区不得重复普通效果、Blossom、Requirements、阶段或版本。

版本属于查询上下文：查询区根据数据集/记录的 `patch` 提供版本选择器，卡片内不重复版本。

## 7. 搜索结果组织

排序优先级建议：

1. 名称完全匹配
2. 名称前缀/高相似匹配
3. 官方/显示效果正文直接命中
4. 条件直接命中
5. 同义词命中
6. 隐藏搜索概念命中
7. 模糊匹配

搜索词命中多个内部概念时，可在结果上方动态显示“你可能在找”的临时分组；这些不是永久首页标签。

## 8. 概率模式

默认关闭。

开启后显示：

- 当前基础候选池数量 N
- 搜索结果命中的目标集合数量 K
- 每个仙灵理论等权概率
- 搜索目标集合理论等权概率

概率分母必须使用 `Candidate Pool`，不能使用 `Displayed Results`。

用户手动排除某仙灵后，该仙灵从 Candidate Pool 移除并实时重算。
概率区同时显示紧凑的已排除列表，支持恢复单个仙灵或清空全部排除，无需刷新页面。

### 8.1 Stage 5+

提供：

- 普通位
- 强制 Combat 位
- 不确定

强制 Combat 位只在当前符合条件的 Combat 候选池中计算等权概率；非 Combat 概率为 0。

“不确定”不要强行平均成一个伪精确单值；UI 可同时显示普通位与强制 Combat 位两组结果。

## 9. 刷新规律页

独立于检索页，整理：

- 官方机制
- 逐仙灵阶段窗口
- reoffer cooldown 观察
- once-per-game
- 特殊要求
- 可负担观察
- Stage 5 Combat 保底
- Blossom 对仙灵的改变
- 尚未确认的抽样权重/类型抽取顺序

每条规则显示可信度。
